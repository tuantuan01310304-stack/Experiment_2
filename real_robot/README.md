# Real Robot
Real mechArm270 implementation using the same ROS 2 trajectory-control workflow.

- `code/`: real-arm driver, kinematics, task node and launch file
- `config/`: driver, grasp and controller parameters
- `logs/`: final execution, 5/5 validation and abnormal joint-limit test
- `screenshots/`: selected terminal evidence from the real-robot stage
